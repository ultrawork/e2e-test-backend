import { Router } from "express";

const notesRouter = Router();

// GET    /api/notes
// GET    /api/notes/:id
// POST   /api/notes
// PUT    /api/notes/:id
// DELETE /api/notes/:id

export { notesRouter };
